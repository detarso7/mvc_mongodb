<h1>Editar produto</h1>
<form action="/products/edit/{{editProduct._id}}" method="POST">
    <div class="form-control">
        <label for="name">Nome do produto:</label>
        <input type="text" name="name" placeholder="Digite o nome do produto" value="{{editProduct.name}}">
    </div>
    <div class="form-control">
        <label for="price">Preço do produto:</label>
        <input type="text" name="price" placeholder="Digite o preço" value="{{editProduct.price}}">
    </div>
        <div class="form-control">
        <label for="name">Imagem do produto:</label>
        <textarea name="image" placeholder="Link da imagem">{{editProduct.image}}</textarea>
    </div>
    <div class="form-control">
        <label for="name">Descrição do produto:</label>
        <textarea name="description" placeholder="Descrição do produto">{{editProduct.description}}</textarea>
    </div>
    <input type="submit" class="btn" value="Atualizar">
</form>